<Window x:Class="KTouch.kBrowser" 
                  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        		  xmlns:s="http://schemas.microsoft.com/surface/2008" 
        
                  Height="1080" 
                  Width="1920"                      
                  Title="kTouch"
                  WindowState="Maximized"
                  WindowStyle="None">
    <Window.Background>
        <!--<ImageBrush ImageSource="/KTouch.Controls;component/Themes/Resources/kTouch_FullScreen.png"/>-->
        <ImageBrush ImageSource="/KTouch.Controls;component/Themes/Resources/fond.jpg"/>
    </Window.Background>
 
    <Window.Resources>
        <ControlTemplate x:Key="FrameTemplate" TargetType="{x:Type Frame}">
            <Grid>
                <DockPanel Name="mask" >
                    <s:SurfaceButton DockPanel.Dock="Left" 
                                     Width="100" 
                                     Height="100" 
                                     Style="{StaticResource NavigationButtonLeftStyle}"
                                     Command="{x:Static NavigationCommands.BrowseBack}" 
                                     IsEnabled="{TemplateBinding CanGoBack}" />
                    <Grid DockPanel.Dock="Right">
                        <s:SurfaceButton  
                                     Width="100" 
                                     Height="100" 
                                     Style="{StaticResource NavigationButtonRightStyle}" 
                                     Command="{x:Static NavigationCommands.BrowseForward}" 
                                     VerticalAlignment="Center"
                                     IsEnabled="{TemplateBinding CanGoForward}"/>
                        <s:SurfaceButton Width="100" 
                                     Height="100" 
                                     Style="{StaticResource NavigationButtonExitStyle}" 
                                     Click="SurfaceButton_Click"
                                     VerticalAlignment="Top"/>
                    </Grid>
                   
                    <ContentPresenter PreviewTouchUp="Navigate"/>
                </DockPanel>
            </Grid>
        </ControlTemplate>
    </Window.Resources>
    <DockPanel>
        <Frame x:Name="_mainFrame" Template="{StaticResource FrameTemplate}"/>
    </DockPanel>
</Window>
